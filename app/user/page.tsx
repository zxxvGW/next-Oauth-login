"use server"
// import { auth, signOut } from "@/auth"
// import { Button } from "@/components/ui/button"
// import { loginWithGoogle } from "../auth/login/action"

export default async function UserPage() {
	console.log(new Date(Date.now()), "server")

	return <div>123</div>
}
